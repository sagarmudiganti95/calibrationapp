<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<MessagePopover items="{oMessageModel>/}" initiallyExpanded="true">
			<MessagePopoverItem type="{oMessageModel>type}" title="{oMessageModel>message}" subtitle="{oMessageModel>additionalText}"
				description="{oMessageModel>description}"/>
	</MessagePopover>
</core:FragmentDefinition>